<h1>Voilà le détail de ce gossip </h1>
<p>Ce Gossip a pour titre "<%= @gossips[@index.to_i].title %>" et a été écrit par <%= link_to "#{@gossips[@index.to_i].user.first_name}", user_page_path(@gossips[@index.to_i].user.id-1) %> le <%= @gossips[@index.to_i].created_at %> </p>
<h2>Voilà son contenu </h2>
<p> <%= @gossips[@index.to_i].content %> </p>



